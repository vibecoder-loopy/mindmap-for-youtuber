# TubeMap Agent Notes

## 제품 방향

TubeMap은 유튜버가 자신의 채널, 영상 아이디어, 레퍼런스, 경쟁 채널, 대본, 성과 분석을 하나의 마인드맵으로 다루는 작업 도구다.

현재 기준은 명확하다.

- 랜딩 페이지나 별도 대시보드를 메인으로 만들지 않는다.
- 지금은 “사용자가 직접 써보고 판단할 수 있는 실제 워크플로우”가 우선이다.
- 기본 제품은 마인드맵 서비스다. AI와 Markdown은 마인드맵을 만들고 정리하기 위한 보조 레이어다.
- 입력, AI 대화, 노드 수정, 그래프 연결, Markdown 편집이 제품의 중심이다.
- 이 프로젝트는 Tauri 앱이 아니라, GitHub에 공개된 오픈소스 웹 프로젝트로 간다.
- 사용자는 저장소를 로컬에 clone한 뒤 `npm install` / `npm run dev`로 실행한다.
- AI 기능은 외부 API 키 입력이 아니라 사용자의 로컬 AI CLI를 호출하는 방식만 우선 지원한다.

## 기억할 핵심 워크플로우 3개

### 1. 하단 고정 AI 대화창

AI는 오른쪽 패널 안에 숨는 보조 기능이 아니라, 화면 하단에 항상 있는 작업 입력창이어야 한다.

사용자는 이 하단 AI 창에 자연어로 명령한다.

예:

- “이 노드 제목 좀 더 클릭 잘 나오게 바꿔줘”
- “이 대본을 7개 챕터로 나눠줘”
- “이 영상 아이디어를 마인드맵으로 확장해줘”
- “선택한 노드랑 관련된 레퍼런스 노드 5개 만들어줘”
- “오른쪽 MD에서 TODO만 정리해줘”
- “이 콘텐츠를 경쟁 유튜버랑 다르게 보이게 고쳐줘”

AI는 단순 답변만 하는 게 아니라 앱 상태를 수정해야 한다.

해야 하는 액션:

- 노드 생성
- 노드 제목/상태/메타 수정
- 노드 삭제
- 노드 간 엣지 생성/삭제
- Markdown 내용 수정
- 오른쪽 MD 패널 열기/닫기
- 분석 결과를 새 노드나 MD 섹션으로 반영

디자인 기준:

- 어두운 작업 도구 톤을 기본으로 하되, 이 문서의 제품 방향을 기준으로 새 UI를 구성한다.
- 하단 중앙 또는 하단 전체 폭에 낮고 조용한 composer를 둔다.
- 채팅 기록은 필요할 때만 확장한다.
- 기본 상태에서는 캔버스를 가리지 않는다.
- Enter는 전송, Shift+Enter는 줄바꿈.

### 2. 유튜브 링크 기반 채널 분석 온보딩

처음 사용자는 자신의 유튜브 채널 또는 영상 링크를 입력한다.

이 링크를 기반으로 앱은 채널 정보를 가져오고, 첫 마인드맵을 자동으로 구성해야 한다.

가져오거나 분석해야 하는 정보:

- 이 사용자가 어떤 유튜버인지
- 어떤 주제의 영상을 만들어왔는지
- 대표 영상과 최근 영상은 무엇인지
- 어떤 영상의 성과가 좋았는지
- 반복되는 키워드와 포맷은 무엇인지
- 경쟁 유튜버는 누구인지
- 경쟁 유튜버의 성과 좋은 콘텐츠는 무엇인지
- 내 채널이 가져갈 수 있는 차별점은 무엇인지
- 앞으로 만들면 좋은 콘텐츠 아이디어는 무엇인지

첫 결과는 단순 리포트가 아니라 마인드맵이어야 한다.

추천 초기 노드 구조:

- 채널 허브 노드
- 내 대표 콘텐츠 노드
- 고성과 영상 노드
- 반복 키워드 노드
- 경쟁 유튜버 노드
- 경쟁 고성과 콘텐츠 노드
- 콘텐츠 갭 노드
- 추천 아이디어 노드
- 다음 제작 TODO 노드

현실적인 구현 단계:

1. 사용자가 유튜브 URL을 넣는다.
2. 우선 URL과 사용자가 붙여넣은 텍스트/설명 기반으로 AI 분석한다.
3. 다음 단계에서 YouTube Data API로 채널/영상 메타데이터를 가져온다.
4. transcript는 자동 수집이 불안정하므로, 사용자가 붙여넣거나 별도 수집 플로우를 둔다.
5. 분석 결과를 노드/엣지/Markdown으로 저장한다.

중요한 제약:

- transcript가 없는데 실제 영상 내용을 본 것처럼 말하면 안 된다.
- URL만 있을 때는 “메타데이터/제공 정보 기반 추정”으로 표시한다.
- AI 서비스 계정/API 인증 정보는 앱 코드나 브라우저에 넣지 않는다.
- 사용자가 로컬에서 이미 로그인해 둔 AI CLI/커맨드를 호출하는 방식이 우선이다.

### 3. 마인드맵과 Obsidian식 그래프

이 서비스는 결국 마인드맵 서비스다. AI 분석이 좋아도 노드와 연결이 직관적이지 않으면 핵심 가치가 약해진다.

반드시 강화해야 할 것:

- 노드 생성/수정/삭제
- 노드 간 연결 생성/삭제
- 노드 간 관계를 빠르게 확인하는 관계도
- 드래그로 위치 조정
- 선택 노드 중심으로 주변 노드 강조
- 연결된 Markdown 백링크 표시
- Map view와 Graph view 모두 유지
- Obsidian처럼 전체 연결망을 한눈에 보는 그래프 제공
- 노드 더블클릭 시 오른쪽 Markdown 열기
- Markdown 안의 `[[wikilink]]`가 실제 노드 연결과 이어지기

노드 타입:

- Channel
- Series
- Video
- Idea
- Script
- Research
- Reference
- Competitor
- Keyword
- Performance
- Todo

노드 타입은 위 목록을 기준으로 시작하고, 실제 워크플로우에서 필요한 타입만 점진적으로 늘린다.

## 오른쪽 Markdown 패널 방향

오른쪽 MD 패널은 실제 작업 도구로 동작해야 하며, 열고 닫기, 수정, 삭제가 되어야 한다.

필수 동작:

- 선택 노드의 Markdown 열기
- 닫기
- 전체화면/축소
- Markdown 직접 수정
- 저장
- 삭제
- 새 Markdown 생성
- AI에게 “이 MD 고쳐줘” 명령 가능
- Markdown의 wikilink를 노드/엣지와 연결
- 선택 노드와 직접 연결된 노드 목록 표시
- 선택 노드의 incoming/outgoing 관계 표시
- Obsidian 백링크처럼 이 Markdown을 참조하는 다른 노드/문서 표시
- Markdown 패널 안에서 작은 관계도 또는 관계 목록을 확인하고 연결된 노드로 이동

저장 방식 우선순위:

1. 우선 `localStorage`로 빠르게 구현
2. 그 다음 Node 기반 로컬 브리지에서 workspace 파일로 저장
3. 장기적으로는 사용자가 clone한 프로젝트 디렉터리 안의 JSON/Markdown 파일을 읽고 쓰는 구조

중요한 방향:

- Tauri 전제의 파일 저장이나 앱 포장은 하지 않는다.
- 브라우저만으로 불가능한 로컬 파일/CLI 작업은 Node dev server 또는 별도 로컬 브리지에서 처리한다.
- 원격 백엔드 DB는 현재 제품 방향이 아니다.
- 사용자의 콘텐츠와 AI 인증 정보는 기본적으로 로컬 환경 밖으로 나가지 않아야 한다.

## 현재 저장소 상태

새 구현은 이 문서를 제품/에이전트 기준으로 삼는다.

처음부터 다시 만들 때 필요한 큰 덩어리:

- 웹 앱 기본 구조
- 마인드맵 캔버스
- Obsidian식 그래프 뷰
- 오른쪽 Markdown 패널
- 하단 고정 AI composer
- 로컬 AI CLI 브리지
- 로컬 저장소
- 오픈소스 사용자가 clone 후 바로 실행할 수 있는 개발 환경

제품 기준:

- 별도 대시보드/랜딩을 메인으로 만들면 안 된다.
- API 키 기반 프록시가 아니라 로컬 AI CLI 호출이 맞다.
- 오른쪽 MD 패널은 실제 편집/저장/삭제가 가능해야 한다.
- 하단 AI composer가 제품의 핵심 조작 레이어가 되어야 한다.

## AI 구현 방향

최종 AI 방향은 API 키 입력 방식이 아니다.

사용자가 개인적으로 구독하거나 로그인해 둔 AI 서비스를 로컬 커맨드로 호출한다. 터미널에서 `codex`를 실행해 쓰는 것처럼, TubeMap도 로컬 프로젝트의 Node 브리지를 통해 해당 CLI를 호출해 결과를 받아와야 한다.

원칙:

- 사용자는 자신의 AI 계정/구독을 로컬 CLI에서 이미 인증해 둔다.
- TubeMap은 API 키를 직접 받거나 저장하지 않는다.
- TubeMap은 로컬 Node 브리지에 요청하고, 브리지는 로컬 커맨드에 프롬프트와 현재 앱 상태를 전달한다.
- CLI가 반환한 결과를 파싱해서 노드/엣지/Markdown 액션으로 적용한다.
- 실패하면 사용자가 터미널에서 직접 같은 명령을 실행해 디버깅할 수 있어야 한다.
- GitHub에 공개되는 기본 코드는 특정 유료 서비스나 비공개 API 키에 종속되면 안 된다.

예상 구조:

```text
TubeMap UI -> Node local CLI bridge -> user's AI CLI -> structured action JSON -> TubeMap state update
```

구현 후보:

- Node 브리지에서 `child_process.spawn()`으로 `codex` 같은 CLI 실행
- 앱 설정에서 사용할 커맨드와 인자를 지정
- 기본값 예시: `codex exec --json` 같은 형태. 실제 사용 가능한 명령은 사용자가 설치한 CLI 기준으로 맞춘다.
- 브리지는 CLI 종류별 어댑터를 둘 수 있지만, 초기 구현은 하나의 command/args 설정으로 충분하다.
- 브라우저 번들에는 `child_process`, API 키, 인증 토큰이 들어가면 안 된다.

중요한 점:

- “개인 AI 구독 계정”을 앱이 직접 로그인 처리하는 것이 아니다.
- 앱은 사용자의 로컬 CLI 세션을 호출한다.
- CLI가 비대화형 출력/JSON 출력을 지원해야 안정적이다.
- JSON 출력이 불안정하면 후처리 파서와 재시도 프롬프트가 필요하다.
- API 키 기반 서버 프록시는 제품 방향이 아니다.

액션 출력은 반드시 구조화한다.

예:

```json
{
  "message": "요청한 내용을 바탕으로 콘텐츠 맵을 확장했습니다.",
  "actions": [
    { "type": "create_node", "node": { "title": "AI 코딩 실패 사례", "kind": "Idea" } },
    { "type": "create_edge", "from": "selected", "to": "new:0" },
    { "type": "update_markdown", "nodeId": "selected", "patch": "..." }
  ]
}
```

## 실행 방법

로컬 실행:

```bash
npm install
npm run dev
```

AI 포함:

```bash
# 예: 사용자가 먼저 터미널에서 AI CLI 로그인을 끝낸다.
codex login

# 그 다음 TubeMap을 실행한다.
npm run dev
```

TubeMap은 실행 중인 로컬 Node 브리지를 통해 설정된 AI CLI 커맨드를 호출한다. AI CLI가 설치되어 있지 않거나 로그인되어 있지 않으면, 앱은 사용자가 터미널에서 재현 가능한 명령과 오류 메시지를 보여줘야 한다.

## 오픈소스 배포 방향

이 저장소는 사용자가 clone해서 자기 컴퓨터에서 돌리는 로컬 우선 프로젝트다.

원칙:

- GitHub 공개 저장소에 올릴 수 있어야 한다.
- `.env`, API 키, 개인 토큰, 사용자 데이터는 커밋하지 않는다.
- 기본 실행 경로는 `npm install` 후 `npm run dev`다.
- AI는 서버 SaaS로 프록시하지 않고, 사용자의 로컬 CLI 인증 상태를 활용한다.
- 프로젝트를 fork한 사람이 다른 CLI를 붙일 수 있도록 command/args 설정을 단순하게 둔다.
- 로컬 파일 저장을 도입할 때도 프로젝트 디렉터리 기준의 명확한 데이터 폴더를 사용한다.

임시 API 프록시 방식은 폐기 방향이다. `.env` 기반 `OPENAI_API_KEY` 흐름은 최종 제품 방향이 아니다.

## 로컬 AI CLI 설정 방향

앱 설정에 다음 값을 둔다.

- AI command: 예 `codex`
- AI args: 예 `exec --json`
- Working directory
- Timeout
- JSON mode on/off
- Dry-run command preview

사용자는 자신의 환경에 맞게 커맨드를 바꿀 수 있어야 한다.

예:

```text
codex exec --json
claude -p
gemini --prompt
```

위 예시는 방향성이다. 실제 명령은 설치된 CLI의 기능에 맞게 검증해서 정한다.

## 다음 구현 순서

1. 하단 고정 AI composer를 만든다.
2. 필요하면 오른쪽 보조 패널은 채팅 결과/상세 로그 패널로 제한한다.
3. AI 응답을 “텍스트 답변”이 아니라 “앱 액션 목록”으로 받는다.
4. 액션 스키마를 만든다.
5. 액션 예시:
   - `create_node`
   - `update_node`
   - `delete_node`
   - `create_edge`
   - `delete_edge`
   - `open_markdown`
   - `update_markdown`
   - `delete_markdown`
6. 유튜브 URL 온보딩 화면 또는 첫 실행 모달을 만든다.
7. 채널 분석 결과를 초기 마인드맵으로 적용한다.
8. 오른쪽 Markdown 패널을 실제 편집 가능하게 만든다.
9. 노드/엣지/Markdown을 `localStorage`에 저장한다.
10. Graph view를 Obsidian 느낌으로 강화한다.

## 구현 시 주의

- 새 화면은 실제 작업 도구처럼 밀도 있고 조용하게 구성한다.
- 하단 AI 창은 항상 보이되 캔버스 작업을 방해하지 않아야 한다.
- 오른쪽 MD 패널은 유지하되 열고 닫기 쉬워야 한다.
- AI는 “추천만 하는 패널”이 아니라 “사용자 대신 앱을 수정하는 조작 레이어”여야 한다.
- 사용자가 직접 귀찮은 수정을 하지 않아도 되게 만드는 것이 핵심이다.
- API 키 기반 구현으로 돌아가지 않는다.
- 로컬 AI CLI 호출은 사용자가 터미널에서 직접 실행 가능한 명령이어야 한다.
